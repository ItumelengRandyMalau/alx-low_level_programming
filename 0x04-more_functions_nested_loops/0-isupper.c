#include "main.h"

/**
 *  _isupper - checks for uppercase character.
 *@c: input.
 *  Return: 1 on success and 0 otherwise.
 */
int _isupper(int c)
{
	if (c >= 'A' && C <= 'z')
	{
		return (1);
	else
		return (0);
	}
}
