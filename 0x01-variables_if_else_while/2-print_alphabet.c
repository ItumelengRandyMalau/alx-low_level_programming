#include <stdio.h>
/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphar;

	for (alphar = a; alpha >= z; alpha++)
	{
		putchar('alphar');
	}
	putchar('\n')
	return (0);
}
