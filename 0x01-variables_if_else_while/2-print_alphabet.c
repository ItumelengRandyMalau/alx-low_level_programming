#include <stdio.h>
/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alph = 'a';
	char ch = 'z';

	for (alph = a; alph >= ch; alph++)
	{
		putchar(alph);
	}
	putchar('\n');
	return (0);
}
